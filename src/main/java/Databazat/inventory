create table inventory(
carid varchar(100),
carname varchar(100),
cartype varchar(100),
carstock int,
carprice varchar(100),
carstatus varchar(100),
carimage varchar(100));

alter table inventory add column dateAdded TIMESTAMP DEFAULT CURRENT_TIMESTAMP;
